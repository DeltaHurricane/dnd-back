{
  "name": "dnd-react",
  "version": "0.1.0",
  "dependencies": {
    "express": "^4.17.1",
    "node": "^12.12.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"fuk u normie\"",
    "dev": "nodemon -w src --exec \"babel-node src --presets env\"",
    "build": "rimraf dist && babel src -s -D -d dist --presets env",
    "start": "node dist"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.19.4",
    "rimraf": "^3.0.0"
  }
}
